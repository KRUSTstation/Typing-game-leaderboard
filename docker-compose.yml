services:
  web:
    build: .
    ports:
      - "8080:5000"
    env_file:
      - .env
    volumes:
      - db-data:/app/instance
    deploy:
      resources:
        limits:
          memory: 128m
        reservations:
          memory: 64m

volumes:
  db-data: